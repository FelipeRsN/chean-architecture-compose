plugins {
    id "com.android.application"
    id "kotlin-android"
}

apply from: "../common.gradle"

android {
    dynamicFeatures = [':features:coin_detail', ':features:coin_list']
}


dependencies {
    api project(":domain")
    api project(":data:repository")
}